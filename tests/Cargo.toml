[package]
name = "solaris-tests"
version = "0.1.0"
authors = ["Maximilian Krüger <kruemaxi@gmail.com>"]

[dependencies]
ethabi = { git = "https://github.com/tomusdrw/ethabi.git", branch = "snd-parse-log-trait" }
ethabi-derive = { git = "https://github.com/tomusdrw/ethabi.git", branch = "snd-parse-log-trait" }
ethabi-contract = { git = "https://github.com/tomusdrw/ethabi.git"}
solaris = { path = "../solaris", version = "0.1" }
ethereum-types = "0.2.2"
rustc-hex = "1.0"

[build-dependencies]
solc = { path = "../solc", version = "0.1" }
